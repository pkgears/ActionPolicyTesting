<!DOCTYPE html>
<html>
  <head>
    <title>ActionPolicyTest</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div>
      <%= link_to("Cerrar sesión", destroy_user_session_path, data:{method: :delete}) if user_signed_in? %>
    </div>
    <%= yield %>
  </body>
</html>
